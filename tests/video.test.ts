import test from 'node:test';
import assert from 'node:assert/strict';
import {MAX_VIDEO_BYTES,parseVideoRange,videoType} from '../lib/video-storage.ts';
const file=(name:string,type:string,size=100)=>({name,type,size});
test('accepts genuine MP4, MOV and WebM headers within 90MB',()=>{const mp4=new Uint8Array([0,0,0,24,102,116,121,112,105,115,111,109,0,0,0,0]);const webm=new Uint8Array([0x1a,0x45,0xdf,0xa3,...Array(12).fill(0)]);assert.equal(videoType(file('m.mp4','video/mp4'),mp4),'video/mp4');assert.equal(videoType(file('m.mov','video/quicktime'),mp4),'video/quicktime');assert.equal(videoType(file('m.webm','video/webm'),webm),'video/webm');assert.equal(videoType(file('m.mp4','video/mp4',MAX_VIDEO_BYTES+1),mp4),null);assert.equal(videoType(file('m.mp4','video/mp4'),webm),null)});
test('range requests support seeking, suffixes and reject invalid positions',()=>{assert.deepEqual(parseVideoRange('bytes=100-199',1000),{offset:100,length:100});assert.deepEqual(parseVideoRange('bytes=950-',1000),{offset:950,length:50});assert.deepEqual(parseVideoRange('bytes=-50',1000),{offset:950,length:50});assert.equal(parseVideoRange('bytes=1000-',1000),false);assert.equal(parseVideoRange('bytes=1-0',1000),false);assert.equal(parseVideoRange('bytes=0-1,3-4',1000),false)});
